# feature-branch-push-pr-merge

- Make sure that I'm in a branch `main`
- If not, bail
- create a new feature branch and name it feat/<name based on current unstaged changes>
- commit all new changes, add a message based on current cchanges, using short bullet points
- push to the feature branch
- create a PR using the same message you used for commit
- merge the PR
